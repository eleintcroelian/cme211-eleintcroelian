import copy,sys,time				#__ Importing the modules to be used.

start_time = time.time()                #__ Starting the timer

if len(sys.argv) >4 or len(sys.argv)<3: #__ Checking if the input arguments are as desired.
        print('Usage: $python3 similarity.py <data_file> <output_file> [user_thresh (default = 5)]\n')
        sys.exit(0)
data_file=copy.copy(sys.argv[1])
output_file=copy.copy(sys.argv[2])
if len(sys.argv)==4:
	user_thresh=copy.copy(sys.argv[3])
else:
	user_thresh=5


data= {}
d=open(data_file)
counter=0
for line in d:
	data[line.split()[0]]=line.split()[1:3]
	counter+=1
	if counter ==5:
		break

