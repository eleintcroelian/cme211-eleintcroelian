import sys, random, copy

if len(sys.argv) <= 4:
	#No arguments, print usage message
	print("Usage:")
	print(" $ python3  generatedata.py <ref_length> <nreads> <read_len> <ref_file> <reads_file>")
	sys.exit(0)

ref_length=copy.copy(sys.argv[1])

nreads=
